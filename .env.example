# Central OBG mail service endpoint that receives the lead and emails Rogue + the customer.
# Defaults to the production URL if unset, so this is optional.
VITE_LEAD_ENDPOINT=https://obg-mail-api.vercel.app/api/send_lead
